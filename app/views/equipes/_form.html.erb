<%= form_with(model: equipe, local: true, html: { class: "form-container" }) do |form| %>
  <% if equipe.errors.any? %>
    <div class="form-errors">
      <h2><%= pluralize(equipe.errors.count, "error") %> prohibited this equipe from being saved:</h2>
      <ul>
        <% equipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :nome, "Nome", class: "form-label" %>
    <%= form.text_field :nome, placeholder: "Nome", class: "form-input" %>
  </div>

  <div class="form-actions">
    <%= form.submit "Salvar", class: "form-button" %>
  </div>
<% end %>
